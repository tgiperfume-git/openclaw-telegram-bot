{
  "name": "openclaw-bot",
  "version": "1.0.0",
  "scripts": {
    "start": "openclaw config set channels.telegram.enabled true && openclaw gateway"
  },
  "dependencies": {
    "openclaw": "latest"
  }
}
